# language: zh-CN
# 设计规格；尚未绑定步骤实现或执行运行器。
功能: 从市场需求到经营利润的可追溯闭环

  @R02 @BR02 @AT02
  场景: 缺少验收条件的诉求不能确认为需求
    假如 一条需求具有来源证据和负责人但没有验收条件
    当 产品负责人确认该需求
    那么 系统拒绝确认并说明缺少验收条件
    并且 需求仍处于已澄清状态

  @R03 @BR04 @AT03
  场景: 过期能力证据不能冒充已核实供给
    假如 资源能力证据已超过其有效期
    当 经营者查看资源匹配结果
    那么 该项能力显示待重新核实
    并且 系统不将其计入已核实的硬条件满足项

  @R04 @BR06 @AT04
  场景: 实验开始后不能改变原成功阈值
    假如 一个实验已启动且冻结了成功阈值
    当 负责人尝试覆盖原成功阈值
    那么 系统拒绝覆盖并要求创建新修订
    并且 原观察记录仍关联原阈值

  @R07 @BR11 @AT07
  场景: 重复消息回调不产生重复业务消息
    假如 同一租户和渠道账号已处理外部事件 event-001
    当 平台再次发送 event-001
    那么 会话中仍只有一条对应业务消息
    并且 下游需求建议不会重复创建

  @R07 @BR11 @AT08
  场景: 发送结果未知时不能直接重发
    假如 消息请求超时且平台可能已接受
    并且 该平台既不支持幂等键也不支持结果查询
    当 发送作业处理超时
    那么 消息被标记为结果未知
    并且 创建人工核对待办而不是再次外发

  @R09 @BR13 @BR14 @AT09
  场景: 内容修改后旧审核不能支持发布
    假如 内容修订 1 已审核且已有排期任务
    当 编辑生成内容修订 2
    那么 修订 2 需要独立审核
    并且 排期任务不得静默切换到修订 2

  @R11 @BR15 @AT11
  场景: 过期报价不能被接受
    假如 报价有效期已过且没有新批准版本
    当 销售记录客户接受该报价
    那么 系统拒绝并提示重新报价或审批
    并且 不产生基于此报价的已确认订单

  @R15 @BR19 @AT14
  场景: 已发布物料清单不能被覆盖
    假如 BOM 修订 R3 已发布
    当 工程师替换其中一个零部件
    那么 系统要求新修订和工程变更记录
    并且 R3 的历史内容保持不变

  @R17 @BR22 @AT16
  场景: 中介收入只使用已确认的佣金口径
    假如 促成交易总额为 100000 元
    并且 经确认的本企业佣金收入为 8000 元
    并且 直接服务成本为 2000 元且可变获客成本为 1000 元
    当 生成不含其他期间费用和税项的经营快照
    那么 毛利为 6000 元且贡献利润为 5000 元
    并且 交易总额不计入本企业佣金收入

  @R17 @BR22 @AT16
  场景: 缺失成本不能被当作零
    假如 一笔订单已收到货款但采购成本尚未核对
    当 用户查看经营结果
    那么 系统显示成本不完整及缺失项
    并且 不显示该结果为最终净利润

  @R18 @BR21 @BR22 @AT17
  场景: 退款产生新的经营快照
    假如 一笔订单已有冻结的利润快照
    当 已核实退款记录被导入两次
    那么 只产生一次业务退款调整
    并且 生成新利润快照并保留旧快照

  @R19 @BR23 @AT18
  场景: ESG 缺失观测不同于实测零
    假如 某期间能源活动数据尚未收到
    当 计算该期间指标
    那么 该观测状态保持缺失
    并且 不自动产生零排放结果

  @R21 @AT20
  场景: 其他租户证据不能进入检索结果
    假如 用户仅获授权访问租户甲
    当 用户按已知标识搜索租户乙的合同证据
    那么 返回结果不包含该证据及其内容
    并且 AI 检索上下文不包含该证据

  @R22 @AT21
  场景: 离线草稿不能覆盖已经批准的新版
    假如 设备保留对象版本 2 的离线草稿
    并且 服务端已批准对象版本 3
    当 设备恢复网络并提交旧版本修改
    那么 系统返回版本冲突
    并且 服务端已批准版本保持不变
